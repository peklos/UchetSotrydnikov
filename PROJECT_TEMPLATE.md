# Шаблон проекта: веб-приложение с каталогом, отзывами и рейтингами

## Основные параметры

- **Название проекта:** `[НАЗВАНИЕ]`
- **Тема:** `[ТЕМА — например: "Рестораны", "Фильмы", "Игры", "Отели", "Курсы"]`
- **Основная сущность:** `[ЧТО ОЦЕНИВАЕМ — например: "рестораны", "фильмы", "товары"]`
- **Язык интерфейса:** русский
- **Тема оформления:** тёмная

---

## Структура проекта

```
[НАЗВАНИЕ]/
├── .github/workflows/build.yml    # CI/CD — сборка Electron-приложения
├── backend/
│   ├── main.py                    # FastAPI — точка входа
│   ├── database.py                # SQLite — подключение и схема
│   ├── models.py                  # Pydantic-модели
│   ├── seed_data.py               # Заполнение БД тестовыми данными
│   ├── requirements.txt           # Python-зависимости
│   └── routers/
│       ├── [сущности].py          # CRUD для основной сущности
│       ├── categories.py          # Категории
│       ├── reviews.py             # Отзывы
│       ├── ratings.py             # Рейтинги и статистика
│       ├── search.py              # Поиск
│       └── admin.py               # Админка
├── frontend/
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       ├── main.js                # Vue-приложение
│       ├── App.vue                # Корневой компонент
│       ├── router.js              # Маршруты
│       ├── api.js                 # Axios-клиент к API
│       ├── components/
│       │   ├── AppHeader.vue      # Навигация
│       │   ├── [Сущность]Card.vue # Карточка сущности
│       │   ├── ReviewCard.vue     # Карточка отзыва
│       │   ├── SearchBar.vue      # Строка поиска
│       │   └── StarRating.vue     # Компонент звёздочек
│       └── views/
│           ├── [Сущности]View.vue       # Каталог
│           ├── [Сущность]DetailView.vue # Детальная страница
│           ├── ReviewsView.vue          # Все отзывы
│           ├── RatingsView.vue          # Рейтинги и статистика
│           ├── SearchView.vue           # Поиск
│           ├── GuideView.vue            # Памятка пользователя
│           └── AdminView.vue            # Админ-панель
├── electron/
│   └── main.js                    # Electron — главный процесс
├── package.json                   # Корневой — Electron + electron-builder
└── build_win.py                   # Локальная сборка (опционально)
```

---

## База данных (SQLite)

### Таблицы

| Таблица | Описание | Ключевые поля |
|---------|----------|---------------|
| `categories` | Категории | `id`, `name`, `description`, `icon` (эмодзи) |
| `brands` | Бренды / Производители / Авторы | `id`, `name`, `country`, `logo_url` |
| `[сущности]` | Основная таблица | `id`, `name`, `description`, `price`/`rating_field`, `image_url`, `category_id` FK, `brand_id` FK, `created_at` |
| `[сущность]_images` | Дополнительные изображения | `id`, `[сущность]_id` FK, `image_url`, `is_main` |
| `reviewers` | Авторы отзывов | `id`, `name`, `email`, `avatar_url`, `registered_at` |
| `reviews` | Отзывы | `id`, `[сущность]_id` FK, `reviewer_id` FK, `rating` (1-5), `title`, `text`, `pros`, `cons`, `created_at`, `is_verified` |
| `review_votes` | Голоса за полезность | `id`, `review_id` FK, `is_helpful` (0/1), `voted_at` |
| `admin_users` | Администраторы | `id`, `username`, `password`, `role` |

> **Примечание:** Поле `price` заменяется на релевантное для темы (например, `duration` для фильмов, `price_range` для ресторанов) или убирается.

---

## Backend (FastAPI)

### API-эндпоинты

#### Основная сущность (`/api/[сущности]`)
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/[сущности]` | Список с фильтрами (`category_id`, `brand_id`, `sort`) |
| GET | `/api/[сущности]/{id}` | Детально + отзывы + изображения + распределение оценок |
| POST | `/api/[сущности]` | Создать |
| PUT | `/api/[сущности]/{id}` | Обновить |
| DELETE | `/api/[сущности]/{id}` | Удалить |

#### Категории и бренды
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/categories` | Список категорий |
| GET | `/api/brands` | Список брендов |

#### Отзывы (`/api/reviews`)
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/reviews` | Все отзывы (фильтры: `sort`, `rating`) |
| POST | `/api/reviews` | Создать отзыв (имя автора + оценка + текст) |
| DELETE | `/api/reviews/{id}` | Удалить |
| POST | `/api/reviews/{id}/vote` | Голос полезно/не полезно |

#### Рейтинги (`/api/ratings`)
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/ratings` | Таблица рейтингов всех сущностей |
| GET | `/api/ratings/stats` | Общая статистика: кол-во, средний рейтинг, распределение, топ-5 лучших/худших |

#### Поиск (`/api/search`)
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/search?q=...` | Поиск по названиям, описаниям, тексту отзывов |

#### Админка (`/api/admin`)
| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/admin/login` | Вход (username + password) |
| GET | `/api/admin/stats` | Статистика для дашборда |
| GET | `/api/admin/reviews` | Все отзывы для модерации |
| DELETE | `/api/admin/reviews/{id}` | Удалить отзыв |
| GET | `/api/admin/[сущности]` | Все сущности для управления |
| GET | `/api/admin/categories` | Категории |
| GET | `/api/admin/brands` | Бренды |

### Зависимости (`requirements.txt`)
```
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.0
```

---

## Frontend (Vue 3 + Vite)

### Страницы и навигация

| Маршрут | Компонент | Описание |
|---------|-----------|----------|
| `/` | Редирект → `/[сущности]` | — |
| `/[сущности]` | `[Сущности]View` | Каталог с фильтрами по категории, бренду, сортировке |
| `/[сущности]/:id` | `[Сущность]DetailView` | Карточка + галерея + отзывы + форма отзыва + распределение оценок |
| `/reviews` | `ReviewsView` | Лента отзывов, фильтр по оценке, сортировка |
| `/ratings` | `RatingsView` | Статистика, диаграмма распределения, топ-5/худшие-5, таблица рейтингов |
| `/search` | `SearchView` | Поиск по сущностям и отзывам |
| `/guide` | `GuideView` | Памятка / инструкция для пользователя (аккордеон + FAQ) |
| `/admin` | `AdminView` | Логин → дашборд (статистика, управление отзывами, управление сущностями: добавить/редактировать/удалить) |

### Компоненты

| Компонент | Описание |
|-----------|----------|
| `AppHeader` | Sticky-навигация с эмодзи-иконками |
| `[Сущность]Card` | Карточка в каталоге: картинка, название, бренд, рейтинг, цена |
| `ReviewCard` | Карточка отзыва: аватар, имя, дата, звёзды, текст, плюсы/минусы, голосование 👍👎 |
| `StarRating` | Звёзды ★ — отображение и интерактивный ввод |
| `SearchBar` | Поле поиска с иконкой 🔍 и кнопкой очистки |

### Зависимости (`frontend/package.json`)
```json
{
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.3.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.4.0"
  }
}
```

---

## Дизайн и UI

- **Тема:** тёмная (`#0d0d1a` фон, `#1a1a2e` карточки, `#e8e8f0` текст)
- **Акцент:** `#4a90d9` (синий) — ссылки, активные элементы
- **Цена/важное:** `#ff6b6b` (красный)
- **Рейтинг-звёзды:** `#f1c40f` (жёлтый)
- **Плюсы:** `#4caf50` (зелёный)
- **Минусы:** `#ff6b6b` (красный)
- **Карточки:** `border-radius: 12px`, тени, hover-эффекты
- **Адаптивность:** CSS Grid, `@media (max-width: 768px)`

---

## Сборка и CI/CD

### GitHub Actions (`build.yml`)

- **Триггер:** push на любую ветку + ручной запуск
- **Матрица:** Windows / macOS / Linux
- **Шаги:**
  1. Checkout
  2. Setup Node.js 20 + Python 3.11
  3. `npm install` + `npm run build` (frontend)
  4. `pip install -r requirements.txt` + `pip install pyinstaller`
  5. PyInstaller: собрать backend в standalone-exe (с frontend/dist внутри)
  6. electron-builder: упаковать в Electron-приложение
  7. Upload artifacts (`.exe` / `.dmg` / `.AppImage`)

### Electron

- `electron/main.js` — запускает backend как дочерний процесс, ждёт готовности, открывает `BrowserWindow`
- `package.json` (корневой) — `electron` + `electron-builder`, `extraResources` для backend

---

## Тестовые данные (`seed_data.py`)

Заполнить при первом запуске:
- **6 категорий** с эмодзи-иконками
- **5 брендов** с названием и страной
- **15-20 сущностей** с описаниями, ценами, картинками-заглушками
- **По 2 изображения** на каждую сущность
- **10 авторов отзывов** с именами и аватарами
- **30-40 отзывов** (разные оценки 1-5, плюсы/минусы, часть verified)
- **Голоса** за полезность (рандом)
- **1 админ:** `admin` / `admin123`

---

## Порядок реализации

### Этап 1: Backend
1. `database.py` — схема таблиц, `init_db()`, `get_db()`
2. `models.py` — Pydantic-модели (Create, Update, Out)
3. `main.py` — FastAPI-приложение, CORS, подключение роутеров, раздача static
4. Роутеры: `[сущности].py`, `categories.py`, `reviews.py`, `ratings.py`, `search.py`, `admin.py`
5. `seed_data.py` — тестовые данные

### Этап 2: Frontend
1. Каркас: `main.js`, `App.vue`, `router.js`, `api.js`
2. Компоненты: `AppHeader`, `StarRating`, `SearchBar`, `[Сущность]Card`, `ReviewCard`
3. Страницы: каталог → детальная → отзывы → рейтинги → поиск → памятка → админка
4. Стили: тёмная тема, адаптивность

### Этап 3: Сборка
1. `electron/main.js`
2. Корневой `package.json`
3. `.github/workflows/build.yml`
4. Тестовый запуск

---

## Правки (заполнить перед отправкой)

> Ниже укажи свои изменения — что добавить, убрать или поменять.

- [ ] **Дополнительные поля сущности:** `[например: "год выпуска", "адрес", "длительность"]`
- [ ] **Дополнительные таблицы:** `[например: "актёры", "ингредиенты", "теги"]`
- [ ] **Убрать из шаблона:** `[например: "бренды не нужны", "убрать цену"]`
- [ ] **Изменить дизайн:** `[например: "светлая тема", "другие цвета"]`
- [ ] **Другое:** `[любые правки]`
